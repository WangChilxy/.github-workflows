name: Generate SRS File

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Generate SRS file
      run: |
        echo "这里应该是生成 .srs 文件的命令"
        # 例如：./your_tool convert rules.json rules.srs
        # 注意：这里需要替换成实际的命令

    - name: Upload SRS file
      uses: actions/upload-artifact@v2
      with:
        name: rules.srs
        path: rules.srs
